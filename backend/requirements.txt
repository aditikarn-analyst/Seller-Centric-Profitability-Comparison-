# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Validation & config ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
SQLAlchemy==2.0.36
alembic==1.14.0

# --- Auth (direct use, not passlib — see §10) ---
PyJWT==2.10.1
bcrypt==4.2.1

# --- Bulk CSV path only (§10) ---
pandas==2.2.3
python-multipart==0.0.20     # FastAPI file uploads (bulk CSV)

# --- Testing ---
pytest==8.3.4
httpx==0.28.1          # FastAPI TestClient dependency
